devops.enokdev.com {
    reverse_proxy /api/* backend:8080

    # Proxy everything else to the frontend nginx (serving static files)
    reverse_proxy /* frontend:80

    encode gzip
    tls {
        # Let Caddy manage TLS (default: Let's Encrypt)
    }
}
